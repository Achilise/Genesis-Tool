#ifndef MODULEHEADER_H
#define MODULEHEADER_H

#include <QString>

class ModuleHeader
{
public:
    ModuleHeader(const QString,const QString);
    ~ModuleHeader();
private:

};
#endif // MODULEHEADER_H
